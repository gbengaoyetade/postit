language: node_js
node_js:
- "stable"
services:
  - postgresql
 before_script:
  - npm install sequelize-cli -g
  - sequelize db:migrate
after_success:
- npm run cover
after_script: 
  - nyc npm test && nyc report --reporter=text-lcov | coveralls